name: NVD CVE Collector

on:
  schedule:
    # Runs daily at 08:00 UTC (staggered from advisory collector)
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  collect-cves:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Run NVD collector
        env:
          # Optional: add NVD_API_KEY secret in repo settings for higher rate limits
          # Get a free key at https://nvd.nist.gov/developers/request-an-api-key
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        run: python scripts/NVDCollector.py

      - name: Configure Git
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Action"

      - name: Commit and push changes
        run: |
          git add public/CaseStudies.json
          if git diff --staged --quiet; then
            echo "No new CVEs to commit"
          else
            git commit -m "sync: nvd cve update - $(date +'%Y-%m-%d %H:%M')"
            git push
          fi
